name: Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout pages repo
        uses: actions/checkout@v4

      - name: Checkout nekte TypeScript SDK
        uses: actions/checkout@v4
        with:
          repository: nekte-protocol/nekte
          path: nekte-source

      - name: Checkout nekte Python SDK
        uses: actions/checkout@v4
        with:
          repository: nekte-protocol/nekte-python
          path: nekte-python

      - uses: pnpm/action-setup@v4
        with:
          version: 9

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install pages dependencies
        run: pnpm install --frozen-lockfile

      - name: Install nekte TS dependencies (for TypeDoc)
        run: pnpm install --frozen-lockfile
        working-directory: nekte-source

      # --- TypeScript API docs ---
      - name: Generate TypeScript API docs
        run: |
          npx typedoc \
            --plugin typedoc-plugin-markdown \
            --out src/content/docs/api/typescript \
            --entryPoints nekte-source/packages/core/src/index.ts \
                          nekte-source/packages/client/src/index.ts \
                          nekte-source/packages/server/src/index.ts \
            --tsconfig nekte-source/tsconfig.json \
            --skipErrorChecking

      - name: Post-process TypeScript API docs
        run: node scripts/process-api-docs.mjs

      # --- Python API docs ---
      - name: Install Python SDK and pdoc
        run: |
          pip install pdoc
          pip install ./nekte-python

      - name: Generate Python API docs
        run: node scripts/generate-python-docs.mjs

      # --- Build & Deploy ---
      - name: Build website
        run: pnpm build

      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

      - id: deployment
        uses: actions/deploy-pages@v4
